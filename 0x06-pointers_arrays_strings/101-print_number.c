#include <stdio.h>
/**
  * print_number - encode a string using rot13
  * @n: the first var
  * Return: return void
  */
void print_number(int n)
{
	printf("%d",n);
	/*
	int i = 0;
	int j = 0;
	int k = 0;
	int l = 0;
	int op = n;

	if (n < 0)
	{
		_putchar('-');
		op *= -1;
	}
	if (op / 10 > 0)
	{
		i = op % 10;
		op /= 10;
		if (op / 10 > 0)
		{
			j = op % 10;
			op /= 10;
			if (op / 10 > 0)
			{
				k = op % 10;
				op /= 10;
				if (op / 10 > 0)
				{
					l = op % 10;
				}
			}
		}
	}
	if (l > 0)
		_putchar('0' + l);
	if (k > 0)
		_putchar('0' + k);
	if (j > 0)
		_putchar('0' + j);
	if (i > 0)
		_putchar('0' + i);*/
}
